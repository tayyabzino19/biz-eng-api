'use strict';

/**
 * conversation-category service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::conversation-category.conversation-category');
