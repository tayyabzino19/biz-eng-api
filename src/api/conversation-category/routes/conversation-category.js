'use strict';

/**
 * conversation-category router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::conversation-category.conversation-category');
